import styled from 'styled-components';
import { SmallHeading } from '../../../styles/styles';

export const StyledLabel = styled(SmallHeading)`
     display: flex;
     align-items: center;
     hr {
       flex: 1;
     }
     label {
       padding: 0 10px;
     }
`;
